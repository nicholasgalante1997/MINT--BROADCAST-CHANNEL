class Trainer {
  id;
  username;
  pokemon;

  constructor(id, username, pokemon) {
    this.id = id;
    this.username = username;
    this.pokemon = pokemon;
  }

  propose_trade(sending_pokemon_id, receiving_pokemon_id, trainer_id) {}
}
